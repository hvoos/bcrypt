using System;
using Bcrypt.Models;
using Microsoft.EntityFrameworkCore;

namespace Bcrypt.Data
{
    public class AppDbContext : DbContext
    {
        public AppDbContext(DbContextOptions<AppDbContext> options)
            : base(options)
        {
        }

        public DbSet<User> Users { get; set; }
    }
}
